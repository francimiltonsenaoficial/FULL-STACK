//Operador de Negação
//exemplo abaixo funciona mas é mt simples

const tem_ingresso = false;

if (tem_ingresso) {

} else { 
    console.log('Não tem ingresso');
}

//exemplo mais usado usando ! (significa não) ele muda o valor logico da variavel (valor booleano)

const temIngresso = false;

    if (!temIngresso) {
        console.log("Não tem ingresso");
    }